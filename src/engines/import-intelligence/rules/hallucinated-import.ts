// ── Hallucinated Import Rule (placeholder) ─────────────────────
// This rule is not currently produced by the import-intelligence engine. The
// file is kept to satisfy the modular layout request.

import type { Diagnostic } from '../../../types/index.js'

export function detectHallucinatedImport(
  _imp?: unknown,
  _relPath?: string,
  _dependencies?: unknown,
  _paths?: unknown,
  _baseUrl?: unknown,
  _rootDirectory?: string,
): Diagnostic[] {
  return []
}
